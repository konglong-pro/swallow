# Final QA Report - 2026-05-17

## Scope

This QA run validates the Swallow ingest-only release gate on the local Windows/Python 3.11 environment.

Runtime QA artifacts were written under `.qa-final/`, which is git-ignored.

## Environment

- Python: 3.11.15
- Platform: Windows 10
- MarkItDown: installed
- PaddleOCR: installed
- faster-whisper: installed
- ffmpeg: resolved through `imageio-ffmpeg`
- Crawl4AI: installed
- Playwright Chromium: installed and launchable
- Firecrawl client: installed
- `FIRECRAWL_API_KEY`: not set

## Commands Run

```powershell
uv run --no-sync swallow doctor --deep --json

uv run --no-sync pytest -q -m batch
uv run --no-sync pytest -q -m api
uv run --no-sync pytest -q -m security
uv run --no-sync pytest -q -m integration
uv run --no-sync pytest -q -m concurrency
uv run --no-sync pytest -q -m network

$env:RUN_PERFORMANCE_TESTS = "1"
uv run --no-sync pytest -q -m performance

uv run --no-sync pytest -q -m "not network and not performance and not heavy"

uv run --no-sync swallow --store .qa-final\batch --config .qa-final\final-qa.config.yaml batch "tests/fixtures/**/*" --workers 2 --json

uv run --no-sync swallow --store .qa-final\direct --config .qa-final\final-qa.config.yaml file tests/fixtures/files/simple.docx
uv run --no-sync swallow --store .qa-final\direct --config .qa-final\final-qa.config.yaml file tests/fixtures/files/electronic.pdf
uv run --no-sync swallow --store .qa-final\direct --config .qa-final\final-qa.config.yaml browser-capture tests/fixtures/browser/chatgpt_capture.json
uv run --no-sync swallow --store .qa-final\direct --config .qa-final\final-qa.config.yaml archive tests/fixtures/archives/chatgpt_export_sample.zip

uv run --no-sync python scripts/runtime_smoke_all.py --store .qa-final\runtime --keep-going --asr-model tiny --asr-device cpu --asr-compute-type int8 --web-url https://example.com/ --web-token Example

uv run --no-sync pytest -q
uv run --no-sync python -m compileall -q src tests scripts

docker --version
```

## Results

| Area | Status | Evidence |
|---|---:|---|
| Doctor | PASS with warnings | Only `FIRECRAWL_API_KEY` and first-run profile directory warnings |
| Batch tests | PASS | `3 passed` |
| API tests | PASS | `20 passed` |
| Security tests | PASS | `6 passed` |
| Fixture integration tests | PASS | `5 passed` |
| Concurrency tests | PASS | `5 passed` |
| Performance tests | PASS conditional | `3 passed`, OCR/ASR performance skipped unless `RUN_OCR_TESTS` / `RUN_ASR_TESTS` are set |
| Firecrawl real API test | CONDITIONAL | skipped unless `RUN_FIRECRAWL_TESTS=1` and `FIRECRAWL_API_KEY` are set |
| Default CI marker gate | PASS | `175 passed`, `1 skipped`, `1 deselected` |
| Full pytest suite | PASS | `175 passed`, `2 skipped` |
| Compile check | PASS | `compileall` succeeded |
| Direct DOCX ingest | PASS | document, trace, manifest generated |
| Direct electronic PDF ingest | PASS | document, trace, manifest generated |
| Direct browser capture ingest | PASS | document, trace, manifest generated |
| Direct ChatGPT archive ingest | PASS | document, trace, manifest generated |
| Runtime service smoke | PASS | file, rerun, browser capture, archive |
| Runtime OCR smoke | PASS | image and scanned PDF |
| Runtime ASR smoke | PASS | generated WAV with `tiny/cpu/int8` |
| Runtime web smoke | PASS | router, Crawl4AI, Playwright, profile |
| Firecrawl real API | CONDITIONAL | skipped because `FIRECRAWL_API_KEY` is not set |
| Docker local smoke | CONDITIONAL | Docker CLI is not installed on this machine |

## Batch Fixture Matrix

Full fixture batch:

- Store: `.qa-final/batch`
- Summary: `.qa-final/batch/batch_runs/batch_20260517_105300_01krsx/summary.json`
- Status: `partial`
- Total: 27
- Success: 7
- Partial: 12
- Failed: 8

The `partial` status is expected for this mixed corpus because it intentionally includes bad files, zip-slip samples, silent/no-speech media, and archive zips passed through the generic file batch command.

Acceptance property verified: one failed input did not stop the batch, and every item has its own job id, manifest path, and trace path.

## Release Blockers

Closed:

- RB-001 batch command
- RB-002 batch summary.json
- RB-003 `/v1` API endpoints
- RB-004 full fixture corpus
- RB-005 path traversal security tests
- RB-006 zip-slip security tests

Conditional or remaining P2:

- RB-007 concurrency: covered by `pytest -m concurrency`, including 20 small files, 5 PDFs, repeated same-file ingest, repeated batch runs, and concurrent batch failure isolation.
- RB-008 performance: formal threshold tests are implemented behind `RUN_PERFORMANCE_TESTS=1`. Local lightweight performance passed; OCR/ASR thresholds remain opt-in with `RUN_OCR_TESTS=1` and `RUN_ASR_TESTS=1`.
- RB-009 Firecrawl real API: implemented behind `RUN_FIRECRAWL_TESTS=1`; skipped locally due missing `FIRECRAWL_API_KEY`.
- Cross-platform/Docker CI: configured in `.github/workflows/ci.yml` and `Dockerfile`; not executed by this local Windows QA run.
- GPU CI: not implemented because current worker defaults are CPU-compatible and no GPU runner is configured.

## Final Judgment

This run qualifies Swallow as a local Release Candidate for the ingest core.

P0, P1, Security Closure, and local P2 hardening are passing in this environment. Full all-environment final release still depends on executing the configured GitHub Actions matrix and providing `FIRECRAWL_API_KEY` for the real Firecrawl gate.
